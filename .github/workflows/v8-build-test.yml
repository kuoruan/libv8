name: V8 Build Test

on:
  push:
    branches:
      - master
      - actions
    paths-ignore:
      - "depot_tools"
      - ".gitignore"
      - ".gitpod*"
  workflow_dispatch:

jobs:
  build-test:
    name: Build V8
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        arch: [x86, x64, arm, arm64]
      exclude:
        - os: macos-latest
          arch: x86
        - os: macos-latest
          arch: arm
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: true
          clean: true
          fetch-depth: 1

      - name: Git Setup and Cleanup
        shell: bash
        run: |
          git config --global core.autocrlf false
          git config --global core.filemode false
          git config --global core.fscache true
          git config --global core.preloadindex true

          git submodule foreach --recursive 'git clean -ffdx'
          git submodule foreach --recursive 'git reset --hard HEAD'

      - name: Restore V8 Cache
        uses: actions/cache@v4
        with:
          path: |
            v8
            .gclient_entries
            .gclient_previous_sync_commits
            .cipd
          key: ${{ runner.os }}-${{ matrix.arch }}:libv8:v8:${{ hashFiles('**/VERSION') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.arch }}:libv8:v8:

      - name: Initialize Archive Name
        if: runner.os != 'Windows'
        shell: bash
        run: ./.github/workflows/scripts/archive-name.sh

      - name: Initialize Archive Name (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: .\.github\workflows\scripts\archive-name.ps1

      - name: Build V8 for Linux (${{ matrix.arch }})
        if: startsWith(matrix.os, 'ubuntu')
        continue-on-error: true
        uses: ./.github/actions/build-linux
        with:
          archive-name: ${{ env.ARCHIVE_NAME }}
          arch: ${{ matrix.arch }}

      - name: Build V8 for macOS (${{ matrix.arch }})
        if: startsWith(matrix.os, 'macos')
        continue-on-error: true
        uses: ./.github/actions/build-macos
        with:
          archive-name: ${{ env.ARCHIVE_NAME }}
          arch: ${{ matrix.arch }}

      - name: Build V8 for Windows (${{ matrix.arch }})
        if: startsWith(matrix.os, 'windows')
        continue-on-error: true
        uses: ./.github/actions/build-windows
        with:
          archive-name: ${{ env.ARCHIVE_NAME }}
          arch: ${{ matrix.arch }}

      - name: Archive V8 Library
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARCHIVE_NAME }}
          path: |
            *.tar.xz
            *.7z
          retention-days: 7
